import React from "react";
import ReactDOM from "react-dom/client";
import DarkMode from "./DarkMode/DarkModeApp";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <DarkMode />
  </React.StrictMode>
);
